import Link from "next/link";
import PasswordInput from "@/components/PasswordInput";

export default async function SuperAdminLogin({ searchParams }: { searchParams: Promise<{ error?: string }> }) {
  const { error } = await searchParams;
  return <main className="legacyAuthPage adminLegacyAuth"><section className="legacyAuthShell"><div className="legacyLogoPane"><img src="/assets/dizons-logo.jpg" alt="DIZON'S Pet Grooming" /></div><div className="legacyLoginPane"><div className="legacyLoginCard"><div className="legacyAuthHeading"><small>SECURE CONTROL CENTER</small><h1>DIZON&apos;S<br />PET GROOMING</h1><h2>SUPER ADMIN</h2><p>Account and administrator management.</p></div><div className="legacyAdminNotice">Super Admin access is separate from the regular admin portal.</div>{error && <div className="authError">{error === "setup-closed" ? "A Super Admin already exists. Please sign in." : "Invalid Super Admin credentials or suspended account."}</div>}<form action="/api/auth/superadmin-login" method="post" className="legacyAuthForm"><label>SUPER ADMIN EMAIL<input type="email" name="email" autoComplete="username" required /></label><label>PASSWORD<PasswordInput /></label><button type="submit">Login as Super Admin</button></form><div className="superAdminEntryLinks"><Link href="/superadmin/setup">First-time Super Admin setup</Link><Link href="/admin/login">Regular admin login</Link></div><Link href="/" className="legacyBackLink">Back to home</Link></div></div></section></main>;
}
